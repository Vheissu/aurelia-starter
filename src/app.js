export class App {
    configureRouter(config, router) {
        config.title = 'Aurelia Starter';

        config.map([
            // Define routes
        ]);

        this.router = router;
    }
}
